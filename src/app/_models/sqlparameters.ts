export class SQLParameters {
    where_field: string;
    where_string: string;
    where_any_position: string;
    sort_field: string;
    sort_direction: string;
    limit_number: string;
    limit_index: string;
}

export class LaunchParameters {
  where_string: string;
  curation_id: number;
}
